/** @jsx React.DOM */
define(function(require) {
  var React = require('react');
  var Cross = React.createClass({
    render: function() {
      return(
        <span>✕</span>
      );
    }
  });

  return Cross;
});